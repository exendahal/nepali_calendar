namespace nepali_calendar_picker.Models
{
    /// <summary>Controls how the date picker is presented on screen.</summary>
    public enum PickerPresentation
    {
        /// <summary>Slides up from the bottom of the screen. Default.</summary>
        BottomSheet,

        /// <summary>Floats as a centered dialog with a scrim behind it.</summary>
        Dialog,
    }
}
